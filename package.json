{
  "name": "mail2tg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mail2tg --file src/storage/schema.sql",
    "self-check": "node scripts/self-check.mjs"
  }
}
